<script>
	import EntityExplorer from '@app/ui/EntityExplorer.svelte';

	export let world;

	let subpage = 0;
</script>

<div class="flex flex-col">
	<label>
		<input type="radio" bind:group={subpage} value={0} />
		show various helpful infos about {$world.title}
	</label>
	<label>
		<input type="radio" bind:group={subpage} value={1} />
		show raw data
	</label>
</div>

{#if subpage === 0}
	TODO show helpful info
{:else}
	<EntityExplorer entity={$world} />
{/if}

{#if $world.content}{$world.content}{/if}
