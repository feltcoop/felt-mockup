<script>
	import WorldName from '@app/world/WorldName.svelte';
	import BlogPost from './BlogPost.svelte';
	import ForumReplies from '@app/forum/ForumReplies.svelte';

	export let posts;
	// $: console.log('blog post list', posts);
</script>

<ul>
	{#each posts as post}
		<!-- <BlogPostListItem {post} /> -->
		<li class="my-10">
			<BlogPost {post} />
			{#if post.children}
				<ForumReplies parent={post} />
			{/if}
		</li>
	{/each}
</ul>
