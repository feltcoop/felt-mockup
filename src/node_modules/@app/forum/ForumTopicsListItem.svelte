<script>
	import WorldName from '@app/world/WorldName.svelte';
	import { symbols } from '@app/ui/symbols.js';

	export let topic;
</script>

<div class="flex">
	{#if topic.author === symbols.avatar}
		<span class="text-2xl leading-none">{symbols.avatar}</span>
	{:else}
		<WorldName name={topic.author} />
	{/if}
	<!-- href={topic.slug} -->
	<a class="ml-4 hover:underline" on:click={() => alert('work in progress!')}>
		<div>{topic.title}</div>
	</a>
</div>
